# 🔐 Create MongoDB Atlas User - Step by Step

## ✅ Step 1: Create Database User in MongoDB Atlas

1. **Go to [MongoDB Atlas](https://cloud.mongodb.com)**
2. **Sign in** to your account
3. **Click "Database Access"** in the left sidebar
4. **Click "Add New Database User"** button

## ✅ Step 2: Configure the User

### Username
**Suggested format:** `renderuser` or `jobtrackeruser`

**Or use:** Any name you prefer (e.g., `backenduser`, `appuser`)

**Important:**
- ✅ Use lowercase letters and numbers only
- ✅ Keep it simple (no special characters)
- ✅ 3-20 characters recommended

### Password
**Click "Autogenerate Secure Password"** - This is the easiest and most secure option!

**OR create your own password:**
- ✅ At least 8 characters
- ✅ Mix of uppercase, lowercase, numbers
- ✅ **NO special characters** (to avoid URL encoding issues)
- ✅ Example: `RenderPass2024` or `JobTracker123`

**Recommended:** Use autogenerate, then copy it immediately!

### User Privileges
**Select:** "Read and write to any database"

**OR** if you want to be more specific:
- Select "Atlas admin" (for full access)

### Authentication Method
**Select:** "Password"

## ✅ Step 3: Save the User

1. **Click "Add User"** button
2. **IMPORTANT:** Copy the password immediately if you autogenerated it!
   - You won't be able to see it again
   - If you lose it, you'll need to reset it

## ✅ Step 4: Get Your Connection String

1. **Go to "Database"** (left sidebar)
2. **Click "Connect"** on your cluster
3. **Choose "Connect your application"**
4. **Select:**
   - Driver: `Node.js`
   - Version: `5.5 or later`
5. **Copy the connection string** - it will look like:
   ```
   mongodb+srv://<username>:<password>@cluster0.abc123.mongodb.net/?retryWrites=true&w=majority
   ```

## ✅ Step 5: Build Your Complete Connection String

**Replace in the connection string:**
1. `<username>` → The username you just created (e.g., `renderuser`)
2. `<password>` → The password you just created
3. Add database name: `/job-tracker` before the `?`

**Example:**
```
mongodb+srv://renderuser:RenderPass2024@cluster0.abc123.mongodb.net/job-tracker?retryWrites=true&w=majority
```

## ✅ Step 6: Update Render

1. **Go to Render Dashboard** → Your Backend Service
2. **Click "Environment"** tab
3. **Find `MONGODB_URI`** variable
4. **Update it** with your complete connection string:
   ```
   mongodb+srv://renderuser:RenderPass2024@cluster0.abc123.mongodb.net/job-tracker?retryWrites=true&w=majority
   ```
5. **Click "Save Changes"**
6. Render will automatically redeploy

## 🔒 Security Best Practices

### ✅ DO:
- Use autogenerated passwords (most secure)
- Use different passwords for different environments
- Keep passwords in environment variables (never in code)
- Use strong passwords (8+ characters, mixed case, numbers)

### ❌ DON'T:
- Use simple passwords like `password123`
- Share passwords in code or documentation
- Use the same password for multiple services
- Include special characters (unless you URL-encode them)

## 🐛 If You Lose the Password

1. **Go to Database Access**
2. **Click on your user**
3. **Click "Edit"** → **"Edit Password"**
4. **Generate a new password**
5. **Update Render** with the new connection string

## 📝 Quick Checklist

- [ ] Created database user in MongoDB Atlas
- [ ] Username: `renderuser` (or your choice)
- [ ] Password: Autogenerated or simple (no special chars)
- [ ] Permissions: Read and write to any database
- [ ] Copied connection string from MongoDB Atlas
- [ ] Replaced `<username>` and `<password>`
- [ ] Added `/job-tracker` before the `?`
- [ ] Updated `MONGODB_URI` in Render
- [ ] No spaces in connection string

## 🎯 Example Connection String Format

**Template:**
```
mongodb+srv://USERNAME:PASSWORD@CLUSTER_NAME.mongodb.net/job-tracker?retryWrites=true&w=majority
```

**Real Example:**
```
mongodb+srv://renderuser:RenderPass2024@cluster0.abc123.mongodb.net/job-tracker?retryWrites=true&w=majority
```

**Where:**
- `renderuser` = Your username
- `RenderPass2024` = Your password
- `cluster0.abc123.mongodb.net` = Your actual cluster name
- `job-tracker` = Database name

## ✅ After Creating User

1. **Test the connection** by updating Render
2. **Check Render logs** - You should see:
   ```
   🔗 MongoDB Connection String (masked): mongodb+srv://renderuser:***@cluster0.abc123.mongodb.net/job-tracker
   [Nest] Connected to MongoDB successfully
   🚀 Backend server running on port 10000
   ```

---

**Follow these steps to create your MongoDB user and connection string!** 🎉

